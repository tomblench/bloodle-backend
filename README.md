Working title: `bloodle`

A polling and voting platform.

## Running

Run with `gradle bootRun`

## Test data

The database does not persist between executions. To populate with test data, run `./test.sh`
